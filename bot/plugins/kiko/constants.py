class PermissionsValues:
    tag__kiko = "kiko"
    tags = [tag__kiko]

class MessageFormats:
    mc_setup__server_ip = "**Install**" + "\n"
    mc_setup__server_ip += "- Go to https://tlauncher.org/en/" + "\n"
    mc_setup__server_ip += "- Click 'DOWNLOAD TL' - select mac/windows/linux" + "\n"
    mc_setup__server_ip += "- Install TLauncher and java if you are asked (create desktop item or shortcut and launcher once finished)" + "\n"
    mc_setup__server_ip += "\n"
    mc_setup__server_ip += "**Download Mods**" + "\n"
    mc_setup__server_ip += "-  Click the button towards the bottom right that says 'TL MODS'" + "\n"
    mc_setup__server_ip += "-  Search for 'Tekkit Classic Reloaded' at the top - click and install" + "\n"
    mc_setup__server_ip += "-  Enter name at the bottom left and play" + "\n"
    mc_setup__server_ip += "\n"
    mc_setup__server_ip += "Enter the server ip: **`{0}`**"